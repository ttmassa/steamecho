<Window 
    x:Class="SteamEcho.App.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:SteamEcho.App"
    mc:Ignorable="d"
    Title="SteamEcho" Height="450" Width="800"
    Background="#323234"
>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Menu -->
        <ListBox Grid.Column="0" ItemsSource="{Binding Games}" Background="#2B2B2C">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Image Source="{Binding IconPath}" Width="32" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding Title}" VerticalAlignment="Center" FontWeight="Bold"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Main Content Area -->
        <Grid Grid.Column="1">

        </Grid>
    </Grid>
</Window>
